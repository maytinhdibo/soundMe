import { StyleSheet } from "react-native";

export const commonStyle = StyleSheet.create({
  header: {
    paddingLeft:3,
    paddingTop:9,
    fontSize: 27,
    fontWeight: "900",
    justifyContent: "center",
    alignItems: "center",
  },
});
